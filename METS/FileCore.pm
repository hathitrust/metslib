
package METS::FileCore;

# Set core file attributes for embedded metadata.
sub set_filecore {
    my $self = shift;
    my $mimetype = shift;
    my $size = shift;
    my $created = shift;
    my $checksum = shift;
    my $checksumtype = shift;
}

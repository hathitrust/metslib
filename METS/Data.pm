package METS::Data;

# Sets the given DOM node as the XML data section.
sub set_xml_data {
    my $self = shift;
    my $xmldata = shift;
}

# Sets the given data as embedded binary data for this metadata section.
sub set_binary_data {
    my $self = shift;
    my $data = shift;
}

